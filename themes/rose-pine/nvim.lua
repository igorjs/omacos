-- SPDX-License-Identifier: Apache-2.0
-- Rose Pine (Main) colorscheme for Neovim.
-- Copied to ~/.config/nvim/theme.lua by `omacos theme set rose-pine`.

require("rose-pine").setup({
  variant = "main",
})
vim.cmd.colorscheme("rose-pine")
